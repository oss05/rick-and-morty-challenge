import React, { Component } from 'react';
import Card from './Card';

class Cards extends Component {
    state = {  }
    render() { 
        return ( <p>Cards works!</p> );
    }
}
 
export default Cards;